import SignupForm from '@/components/auth/SignupForm';

export const metadata = {
  title: 'Create Account — SubscriptionHub',
  description: 'Sign up and start your subscription journey with SubscriptionHub.',
};

export default function SignupPage() {
  return <SignupForm />;
}
