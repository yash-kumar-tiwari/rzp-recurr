import LoginForm from '@/components/auth/LoginForm';

export const metadata = {
  title: 'Sign In — SubscriptionHub',
  description: 'Sign in to manage your SubscriptionHub subscription.',
};

export default function LoginPage() {
  return <LoginForm />;
}
